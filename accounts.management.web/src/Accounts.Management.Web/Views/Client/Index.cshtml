@model PaginatedResponse<ClientResponse>
@{
    ViewData["Title"] = "Client";
}
<nav class="navbar bg-body-tertiary">
  <div class="container-fluid">
    <h1 class="navbar-brand fs-3">Client</h1>
    <div class="d-flex">
      @if(User.IsInRole(RoleConstants.ClientRole.Create))
      {
        <a asp-action="Create" class="btn btn-success">Add</a>
      }
    </div>
  </div>
</nav>
<table class="table">
  <thead>
    <tr>
      <th scope="col">Name</th>
      <th scope="col">Status</th>
      <th scope="col"></th>
    </tr>
  </thead>
  <tbody>
    @if(Model.Items != null && Model.Items.Count > 0){
        @foreach(var client in Model.Items.OrderBy(o => o.Name))
        {
            <tr>
                <td class="align-middle">@client.Name</td>
                <td class="align-middle">@client.Status.GetDisplayName()</td>
                <td class="d-flex flex-row-reverse">
                    <div class="btn-group" role="group" aria-label="actions">
                        <a asp-action="Details" asp-route-id="@client.Id" class="btn btn-outline-secondary">Details</a>
                        @if(User.IsInRole(RoleConstants.ClientRole.Update))
                        {
                          <a asp-action="Edit" asp-route-id="@client.Id" class="btn btn-outline-secondary">Edit</a>
                        }
                        @if(User.IsInRole(RoleConstants.ClientRole.Delete))
                        {
                          <a asp-action="DeleteConfirm" asp-route-id="@client.Id" class="btn btn-outline-secondary">Delete</a>
                        }
                    </div>
                </td>
            </tr>
        }
    }else{
        <tr>
            <td colspan="3">No records found!</td>
        </tr>
    }
  </tbody>
</table>
@await Html.PartialAsync("_Pagination", new PaginationViewModel{
  HasPreviousPage = Model.HasPreviousPage,
  HasNextPage = Model.HasNextPage,
  PageIndex = Model.Request.PageIndex,
  TotalItems = Model.TotalItems,
  TotalPages = Model.TotalPages
})

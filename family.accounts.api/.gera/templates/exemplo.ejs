/*
    EJS documentation:  
    https://ejs.co/#docs
*/

/*
    Exemple props:
*/
const <%= props.name %>_const:string = '<%= props.name %>'


class <%= props.name %>_class{
    private <%= props.name %>_private:any;

    <%= props.name %>_funct() {
        return '<%= props.name %>'
    }
}
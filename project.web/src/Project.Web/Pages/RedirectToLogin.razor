@inject NavigationManager Nav
@page "/redirect/login"
@using Microsoft.Extensions.Options

@inject IConfiguration Config

@code {
    protected override void OnInitialized()
    {
        Nav.NavigateTo(Config["LoginWebUrl"] + "/login?appSlug=project-api");
    }
}
